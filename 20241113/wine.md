```julia
 # for patterns ∈ bitsearch(an)
    for patterns ∈ [[1,5]]
        @show patterns
        # arr = []
        # for elem ∈ patterns
        #     push!(arr, reduce(|, elem))
        # end

        # @show patterns, arr

        # result = min(result, reduce(⊻, arr))
    end


    # println(result)
```
